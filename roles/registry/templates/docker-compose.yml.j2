version: '3.5'
services:
  api:
    image: registry:latest
    ports:
    - "5000:5000"
    volumes:
    - registry:/var/lib/registry
    restart: always
{% if is_mirror %}
    environment:
      REGISTRY_PROXY_REMOTEURL: {{ mirror_url }}
{% endif %}

volumes:
  registry: